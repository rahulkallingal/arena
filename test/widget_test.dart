// Minimal placeholder test. The app's screens need a live Firebase connection,
// so they aren't unit-tested here; this just keeps `flutter test` green.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('app smoke placeholder', () {
    expect(1 + 1, 2);
  });
}
